# Post-GWAS (V2G/MR/Drug) domain extras.
pandas
numpy
scipy
openpyxl
pyarrow
pyyaml
networkx
matplotlib>=3.7.0
pyvis

# Real-mode steps (cojo.sh, extract_ld_and_run_susie.sh, susie_finemap.R,
# run_mr_pipeline.sh, prs_clump_score.sh) additionally require two conda
# environments -- not installable via pip, see CLAUDE.md / README.md
# "Environment" section:
#   gwasagent  -- Python 3.11+, this file's packages, runs the pure-Python tools
#   finemap    -- gcta64, plink, R + susieR, TwoSampleMR
#                 (plink also drives the optional Stage PRS clumping+scoring)
